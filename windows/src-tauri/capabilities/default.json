{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "The main window. The site is a remote origin and is granted nothing: it never calls into the app.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
