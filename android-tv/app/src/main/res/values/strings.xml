<?xml version="1.0" encoding="utf-8"?>
<resources>
    <string name="app_name">ChillFlixVibes</string>

    <!-- Origin of your ChillFlixVibes deployment. The app calls its
         `/api/tmdb/...` and `/api/anime-id/...` route handlers, so the TMDB
         token stays on the server and is never shipped in the APK.
         No trailing slash. Point this at a LAN dev server
         (e.g. http://192.168.1.20:3000) while developing — if you do, also set
         android:usesCleartextTraffic="true" on <application> in the manifest. -->
    <string name="api_base_url">https://chillflixvibes.vercel.app</string>
</resources>
